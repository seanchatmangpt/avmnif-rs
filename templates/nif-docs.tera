# Generated by ggen from {{ ontology_file }}
# NIF Module Documentation: {{ module_name }}

Generated: {{ generation_time }}

## Overview

This module contains {{ nif_functions | length }} NIF function(s) auto-generated from RDF ontologies.

{% for category in categories -%}

## {{ category }}

{% for nif in nif_functions | filter(attribute="category", value=category) -%}

### {{ nif.label }} (`{{ nif.erlangName }}/{{ nif.arity }}`)

**Rust name**: `{{ nif.rustName }}`

{{ nif.description }}

#### Parameters

{% if nif.parameters | length > 0 -%}
| Name | Type | Description |
|------|------|-------------|
{%- for param in nif.parameters %}
| `{{ param.name }}` | {{ param.type }} | {{ param.doc }} |
{%- endfor %}
{%- else -%}
(No parameters)
{%- endif %}

#### Return Value

Type: **{{ nif.returnType }}**

{%- if nif.canFail %}

#### Error Handling

This function can fail and return an error. Common error conditions:
- Invalid argument types
- Result overflow
- Resource exhaustion
{%- endif %}

#### Examples

{% if nif.examples -%}
```erlang
{%- for example in nif.examples %}
{{ example }}
{%- endfor %}
```
{%- else -%}
(No examples available)
{%- endif %}

---

{% endfor -%}

{% endfor %}

## Testing

Each NIF function has comprehensive tests covering:
- Basic functionality
- Argument validation
- Error conditions
- Edge cases

Run tests with:

```bash
cargo test {{ module_name }}
```

## Code Generation

These NIFs were generated from the RDF ontology at `{{ ontology_file }}`.

To regenerate this module, run:

```bash
ggen sync
```

### Generated Files

- `src/generated/{{ module_name }}/nifs.rs` - Function implementations
- `src/generated/{{ module_name }}/collection.rs` - NIF registration
- `src/generated/{{ module_name }}/tests.rs` - Test suite
- `docs/generated/{{ module_name }}.md` - This documentation

### Modification Policy

**DO NOT manually edit generated code.** Instead:

1. Update the RDF ontology (`schema/*.ttl`)
2. Modify Tera templates (`templates/*.tera`)
3. Run `ggen sync` to regenerate

All manual changes will be lost on the next sync.
