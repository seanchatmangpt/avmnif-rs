% Generated by ggen from {{ ontology_file }}
% Erlang module for {{ module_name }} NIFs
% DO NOT EDIT - This file is auto-generated from RDF ontologies

-module({{ erlang_module_name }}).
-export([
{%- for nif in nif_functions %}
    {{ nif.erlangName }}/{{ nif.arity }}{{ "," if not loop.last else "" }}
{%- endfor %}
]).

%% ─────────────────────────────────────────────────────────────────
%% NIF Function Stubs
%% ─────────────────────────────────────────────────────────────────

{% for nif in nif_functions %}
%% @doc {{ nif.label }}
%%
%% {{ nif.description }}
-spec {{ nif.erlangName }}(
{%- for param in nif.parameters %}
    {{ param.name }}{{ "," if not loop.last else "" }}
{%- endfor %}
) ->
{%- if nif.returnType == "SmallInt" %}
    integer()
{%- elif nif.returnType == "Atom" %}
    atom()
{%- elif nif.returnType == "List" %}
    list()
{%- elif nif.returnType == "Tuple" %}
    tuple()
{%- elif nif.returnType == "Binary" %}
    binary()
{%- elif nif.returnType == "Map" %}
    map()
{%- elif nif.returnType == "Float" %}
    float()
{%- elif nif.returnType == "Pid" %}
    pid()
{%- elif nif.returnType == "Port" %}
    port()
{%- elif nif.returnType == "Resource" %}
    term()
{%- else %}
    term()
{%- endif %}
{%- if nif.canFail %} | {error, term()}{% endif %}.

{{ nif.erlangName }}(
{%- for param in nif.parameters %}
    {{ param.name }}{{ "," if not loop.last else "" }}
{%- endfor %}
) ->
    % This function will be implemented as a NIF
    erlang:nif_error(not_implemented).

{% endfor %}
